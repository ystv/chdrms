[workspace]
resolver = "3"
members = [
    "backend/database/",
    "backend/database_macros/",
    "backend/server/",
]

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
strip = "symbols"
